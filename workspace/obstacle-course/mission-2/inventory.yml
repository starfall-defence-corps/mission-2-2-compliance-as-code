# The SSH key path comes from ansible.cfg (present in the project root),
# so this inventory works whether Ansible is run from there or elsewhere.
all:
  children:
    cis_targets:
      hosts:
        cis-target:
          ansible_host: localhost
          ansible_port: 2251
